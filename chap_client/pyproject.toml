[project]
name = "chap-client"
version = "0.0.1"
description = "HTTP client for the chap REST API. Currently a stub; future home of the client extracted from chap-scheduler."
readme = "README.md"
authors = [{ name = "Morten Hansen", email = "morten@dhis2.org" }]
license = { text = "AGPL-3.0-or-later" }
requires-python = ">=3.13"

[build-system]
requires = ["uv_build>=0.9.0,<0.12.0"]
build-backend = "uv_build"
